{
	"name": "ovh-ikvm-mcp",
	"version": "0.1.0",
	"description": "MCP server for bare metal server iKVM/IPMI console access — screenshot capture for LLM-based debugging",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"dev": "bun run --watch src/index.ts",
		"start": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write ."
	},
	"license": "Apache-2.0",
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.1",
		"jpeg-js": "^0.4.4",
		"pngjs": "^7.0.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.4",
		"@types/bun": "^1.2.0",
		"@types/jpeg-js": "^0.3.7",
		"@types/pngjs": "^6.0.5",
		"typescript": "^5.7.0"
	}
}
